# 羽見 Parrot Atlas

直接開啟 `index.html` 即可瀏覽。

## 檔案
- `index.html`：網站結構與 3 個範例條目
- `styles.css`：完整 RWD 與視覺樣式
- `script.js`：篩選、隨機條目、Modal 互動
- `design.md`：設計系統與擴充規範

若以本機伺服器開啟，可使用：

```bash
python -m http.server 8000
```
